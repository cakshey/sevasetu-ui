rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow reads and writes for now (MVP / testing)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
